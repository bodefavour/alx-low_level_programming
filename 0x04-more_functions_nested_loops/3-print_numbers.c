#include "main.h"

/**
 * print_numbers - a function that prints the numbers,
 * @g:  represented integer used 
 * Return: no return
 */

void print_numbers(void)

{
	int g;

	for (g = 48; g <= 57; g++)
	{
		_putchar(g);
	}

	_putchar('\n');
}
